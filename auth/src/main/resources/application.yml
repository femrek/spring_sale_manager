spring:
  application:
    name: AUTH-SERVICE

eureka:
  client:
    service-url:
      defaultZone : http://${EUREKA_HOST:localhost}:8761/eureka/

server:
  port: 9898
  servlet:
    context-path: /auth
